% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/denoise_map.R
\name{make_denoise_map}
\alias{make_denoise_map}
\alias{make_denoise_map.data.frame}
\alias{make_denoise_map.matrix}
\alias{make_denoise_map.uc_cluster}
\alias{make_denoise_map.list}
\title{Map denoiser-local ASV ids to a master sequence list}
\usage{
make_denoise_map(x, seqs, rc = FALSE)

\method{make_denoise_map}{data.frame}(x, seqs, rc = FALSE)

\method{make_denoise_map}{matrix}(x, seqs, rc = FALSE)

\method{make_denoise_map}{uc_cluster}(x, seqs, rc = FALSE)

\method{make_denoise_map}{list}(x, seqs, rc = FALSE)
}
\arguments{
\item{x}{(\code{data.frame} as returned by \code{dada2::mergePairs()}, integer
matrix as returned by \code{dada2::makeSequenceTable()}, \code{uc_cluster} as
returned by \code{\link[=vsearch_cluster_unoise2]{vsearch_cluster_unoise2()}}, or a named list of one of these)}

\item{seqs}{(\code{character} vector, file name, or \code{Biostrings::XStringSet})
master list of sequences}

\item{rc}{(\code{logical}) if \code{TRUE}, sequences in \code{x} are reverse-complemented
before matching}
}
\value{
\code{data.frame} with columns:
\itemize{
\item \code{sample} (character; present for list and matrix methods)
\item \code{denoise_idx} (integer) denoiser-local ASV id: row index in
\code{mergePairs} output, column index in a sequence table, or
\code{uc_cluster$clusters$clust_idx} (0-based)
\item \code{seq_idx} (integer) index into \code{seqs}; \code{NA} if unmatched
\item \code{nread} (integer) abundance
}
}
\description{
One row per denoiser ASV (per sample when \code{x} is a list). Matching against a
large \code{seq_all} is done once per call; pass the result to
\code{\link[=denoise_map_to_seqtable]{denoise_map_to_seqtable()}} and to \code{\link[=dada2_read_map]{dada2_read_map()}} /
\code{\link[=unoise_read_map]{unoise_read_map()}}.
}
\details{
Rows with unmatched \code{seq_idx} (\code{NA}) are retained so fate maps can
distinguish "denoised but missing from \code{seq_all}" from "never denoised".
}
\seealso{
\code{\link[=denoise_map_to_seqtable]{denoise_map_to_seqtable()}}, \code{\link[=make_mapped_sequence_table]{make_mapped_sequence_table()}}
}
