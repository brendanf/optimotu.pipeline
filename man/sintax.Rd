% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vsearch.R
\name{sintax}
\alias{sintax}
\title{Perform taxonomic classification using SINTAX in VSEARCH}
\usage{
sintax(
  query,
  ref,
  ncpu = NULL,
  id_is_int = FALSE,
  hash = NULL,
  files = NULL,
  seq_idx = NULL,
  vsearch = find_vsearch(),
  ...
)
}
\arguments{
\item{query}{(\code{data.frame}, \code{\link[Biostrings:XStringSet-class]{DNAStringSet}},
\code{character} vector, or file name) query sequences. File inputs may be plain
FASTA or gzipped FASTA.}

\item{ref}{(\code{data.frame}, \code{\link[Biostrings:XStringSet-class]{DNAStringSet}},
\code{character} vector, or file name) reference sequences}

\item{ncpu}{(\code{integer} count) number of threads passed to vsearch
(\code{--threads}), or \code{NULL} to omit the flag.}

\item{id_is_int}{(\code{logical} flag) if \code{TRUE}, return the sequence IDs as
integers}

\item{hash}{(\code{character} string) hash value for the queries; ignored (but
used by \code{targets} for dependency tracking)}

\item{files}{(\code{character} vector) optional per-file paths overriding those
stored in the index, if \code{query} is a
\code{\link[fastqindexr:create_index]{fastqindexr_index}} object or \code{.fqi}
path(s). Useful after moving inputs or for \code{targets} dependency tracking.}

\item{seq_idx}{(\code{integer} vector) optional 1-based indices into the logical
sequence stream (\code{NULL} means all sequences in order). Applies after
concatenating multiple FASTA inputs, and supports duplicates and
reordering.}

\item{vsearch}{(\code{character}) path to the \code{vsearch} executable}

\item{...}{currently unused; reserved for future extensions.}
}
\value{
\code{tibble::tibble} with columns \code{seq_id} (or \code{seq_idx} if \code{id_is_int}
is TRUE), \code{rank}, \code{parent_taxonomy}, \code{taxon}, and \code{prob}, where \code{seq_id}
(\code{seq_idx}) is the ID of a sequence from \code{query}, \code{rank} is the taxonomic
rank of the taxon, \code{parent_taxonomy} is the parent taxonomic unit of the
taxon, \code{taxon} is the taxon name, and \code{prob} is the probability of the
taxon being the correct classification of the sequence. Empty query inputs
return an empty tibble with the same schema.
}
\description{
Perform taxonomic classification using SINTAX in VSEARCH
}
