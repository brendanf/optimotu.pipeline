% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vsearch.R
\name{merged_filter_options}
\alias{merged_filter_options}
\title{Class to store merged-fastq filter options for VSEARCH/USEARCH}
\usage{
merged_filter_options(
  maxEE = 1,
  maxEE_rate = NULL,
  maxNs = NULL,
  maxLen = NULL,
  minLen = NULL
)
}
\arguments{
\item{maxEE}{(\code{numeric} scalar) maximum absolute number of expected errors
allowed in merged read pairs.}

\item{maxEE_rate}{(\code{numeric} scalar) maximum number of expected errors
allowed in merged read pairs, as a fraction of the sequence length.}

\item{maxNs}{(\code{integer} scalar) maximum number of "N"s allowed in merged
read pairs.}

\item{maxLen}{(\code{integer} scalar) maximum length allowed for merged read
pairs.}

\item{minLen}{(\code{integer} scalar) minimum length allowed for merged read
pairs.}
}
\value{
An object of class \code{merged_filter_options}.
}
\description{
In all cases, merged read pairs which do not meet the filter criteria are
excluded.
}
\details{
Called with no arguments, this returns the values parsed from
\code{pipeline_options.yaml} when available, otherwise the constructor defaults.

It appears that vsearch does not count "N"s towards expected errors, even
though their quality score looks like each one represents 0.63 expected
errors. Thus it is possible to allow sequences with N's while still having 0
maximum expected errors.
}
