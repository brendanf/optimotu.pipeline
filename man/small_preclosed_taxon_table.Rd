% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster.R
\name{small_preclosed_taxon_table}
\alias{small_preclosed_taxon_table}
\title{Calculate a pre-closed-reference table for taxa with few sequences}
\usage{
small_preclosed_taxon_table(
  known_taxon_table,
  asv_taxsort,
  rank,
  parent_rank,
  tax_ranks,
  min_ops = cluster_min_parallel_ops(),
  max_ops = cluster_max_batch_ops()
)
}
\arguments{
\item{known_taxon_table}{(\code{data.frame}) a table of known taxon assignments}

\item{asv_taxsort}{(\code{data.frame}) a table of ASV taxonomic assignments.
Must include \code{seq_idx} (integer, 1-based index after taxonomic sorting)
and exactly one of \code{seq_idx_in} (1-based index before sorting) or
\code{seq_id} (character sequence identifier). When using \code{seq_id}, the taxon
table must include \code{seq_id}; a taxon table with only \code{seq_idx} is not
allowed.}

\item{rank}{(\code{character}) the taxonomic rank to calculate}

\item{parent_rank}{(\code{character}) the taxonomic rank to use as a parent}

\item{tax_ranks}{(\code{character}) the taxonomic ranks to use}

\item{min_ops}{(\code{numeric} scalar) pairwise comparisons at or below which a
parent taxon is treated as "small". Should match \code{min_ops} passed to
\code{\link[=large_preclosed_taxon_table]{large_preclosed_taxon_table()}}. Default \code{\link[=cluster_min_parallel_ops]{cluster_min_parallel_ops()}}.}

\item{max_ops}{(\code{numeric} scalar) maximum pairwise comparisons to pack into
one execution group. Default
\code{\link[=cluster_max_batch_ops]{cluster_max_batch_ops()}}, which depends on the
clustering distance method (~10-20 min per batch).}
}
\value{
(\code{data.frame}) a table of sequences for which closed-reference
clustering is required. This includes both sequences which are not assigned
to the target rank which will act as queries, and identified sequences
which belong to the same parent rank as one or more queries. The results
includes a \code{tar_group} column for dynamic branching over execution groups.
If there are no queries, a single row is returned to branch over.
}
\description{
This calculates a pre-closed-reference table for taxa with few sequences,
and groups them into approximately equally sized execution groups.
}
