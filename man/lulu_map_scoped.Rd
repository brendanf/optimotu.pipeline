% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lulu_long.R
\name{lulu_map_scoped}
\alias{lulu_map_scoped}
\title{Scoped LULU parent mapping for one partition grain}
\usage{
lulu_map_scoped(
  stats,
  match_table,
  scope = c("batch", "seqrun", "global"),
  max_dist = lulu_max_dist(),
  min_abundance_ratio = lulu_min_abundance_ratio(),
  min_cooccurrence_ratio = lulu_min_cooccurrence_ratio(),
  use_mean_abundance_ratio = lulu_use_mean_abundance_ratio(),
  verbose = 0L
)
}
\arguments{
\item{stats}{(\code{data.frame} or target expression) output of \code{\link[=lulu_otu_stats]{lulu_otu_stats()}}}

\item{match_table}{expression containing one or more match-table targets}

\item{scope}{(\code{character}) \code{"batch"}, \code{"seqrun"}, or \code{"global"}}

\item{max_dist}{(\code{numeric} scalar) maximum pairwise distance for two OTUs to
be considered for merging}

\item{min_abundance_ratio}{(\code{numeric} scalar) minimum parent:child abundance
ratio for two OTUs to be considered for merging}

\item{min_cooccurrence_ratio}{(\code{numeric} scalar) minimum ratio of
co-occurrences to total occurrences of the child for two OTUs to be
considered for merging}

\item{use_mean_abundance_ratio}{(\code{logical} flag) if \code{TRUE},
\code{min_abundance_ratio} is the mean over co-occurring samples; if \code{FALSE},
the minimum across those samples}

\item{verbose}{(\code{integer}) verbosity level}
}
\value{
a two-column \code{data.frame} with non-identity \code{seq_idx} / \code{lulu_idx}
}
\description{
Decides LULU parents only for OTUs whose grain matches \code{scope}. Intended for
targets with \code{retrieval = "none"}. Returns sparse non-identity rows;
\code{\link[=lulu_map_combine]{lulu_map_combine()}} builds the full map.
}
\details{
When \code{min_cooccurrence_ratio == 1}, pairs whose parent grain is strictly
finer than the child's (parent restricted to a narrower OTU-table
partition) are skipped. OTUs with \code{occurrence == 1} use a singleton
fast-path and are not inserted into the pair map.
}
