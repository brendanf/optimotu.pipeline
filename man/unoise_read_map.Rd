% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unoise.R
\name{unoise_read_map}
\alias{unoise_read_map}
\title{Map the fate of individual reads through UNOISE merge and denoising}
\usage{
unoise_read_map(sample, fq_raw, fq_trim, fq_merged, uc, denoise_map)
}
\arguments{
\item{sample}{(\code{character}) sample name(s)}

\item{fq_raw}{(\code{character}) raw FASTQ R1 file path(s)}

\item{fq_trim}{(\code{character}) trimmed FASTQ R1 file path(s)}

\item{fq_merged}{(\code{character}) merged and filtered FASTQ file path(s)}

\item{uc}{(\code{uc_cluster} or named list of such) result of
\code{\link[=vsearch_cluster_unoise2]{vsearch_cluster_unoise2()}} for the sample(s)}

\item{denoise_map}{(\code{data.frame}) as returned by \code{\link[=make_denoise_map]{make_denoise_map()}} for
the same \code{uc} object(s); must include \code{denoise_idx} and \code{seq_idx}, and
\code{sample} when mapping more than one sample}
}
\value{
\code{data.frame} with columns:
\itemize{
\item \code{sample} (character) the sample name
\item \code{raw_idx} (integer) the index of the sequence in the raw file
\item \code{seq_idx} (integer) the index of the sequence in \code{seq_all}
\item \code{flags} (raw) bitset indicating the presence of the sequence at different
stages:
0x01 = trimmed
0x02 = merged and quality-filtered
0x04 = denoised
0x08 = survived UNCROSS (set later by \code{\link[=add_uncross_to_read_map]{add_uncross_to_read_map()}} when
\code{is_tag_jump} is \code{FALSE}; not set here)
}

Bits \code{0x10}--\code{0x80} are reserved for ASV-level filter results
(\code{asv_map$result}: chimera/spike/model), not per-read fate flags.
}
\description{
Analog of \code{\link[=dada2_read_map]{dada2_read_map()}} for the UNOISE path, where merging and quality
filtering happen before denoising.
}
\details{
Accepts a single sample or a chunk of samples. Sequence-to-index matching
against \code{seq_all} must already have been done by \code{\link[=make_denoise_map]{make_denoise_map()}};
pass that result as \code{denoise_map}.

Bit \code{0x02} (filter) is set when the raw read is present in the merged,
quality-filtered FASTQ. Bit \code{0x04} (denoise) is set when that merged
sequence maps to an ASV in \code{uc}.

Dereplication is repeated here on \code{fq_merged}. That duplicates work already
done inside \code{\link[=vsearch_cluster_unoise2]{vsearch_cluster_unoise2()}}, which is an intentional tradeoff
between computation (dereplication is fast) and storage (the dereplication
map is large).
}
