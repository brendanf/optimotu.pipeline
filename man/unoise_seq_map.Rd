% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unoise.R
\name{unoise_seq_map}
\alias{unoise_seq_map}
\title{Map the fate of individual reads through UNOISE merge and denoising}
\usage{
unoise_seq_map(sample, fq_raw, fq_trim, fq_merged, uc, seq_all, rc = FALSE)
}
\arguments{
\item{sample}{(\code{character}) name of the sample}

\item{fq_raw}{(\code{character}) name of the raw FASTQ R1 file}

\item{fq_trim}{(\code{character}) name of the trimmed FASTQ R1 file}

\item{fq_merged}{(\code{character}) name of the merged and filtered FASTQ file}

\item{uc}{(\code{uc_cluster}) result of \code{\link[=vsearch_cluster_unoise2]{vsearch_cluster_unoise2()}} for this
sample}

\item{seq_all}{(\code{character} vector of sequences,
\code{\link[Biostrings:XStringSet-class]{XStringSet}}, or a readable FASTA path,
e.g. a \code{tar_file} target) unique ASV sequences}

\item{rc}{(\code{logical}) if \code{TRUE}, centroid sequences in \code{uc} are
reverse-complemented relative to \code{seq_all}}
}
\value{
\code{data.frame} with columns:
\itemize{
\item \code{sample} (character) the sample name
\item \code{raw_idx} (integer) the index of the sequence in the raw file
\item \code{seq_idx} (integer) the index of the sequence in \code{seq_all}
\item \code{flags} (raw) bitset indicating the presence of the sequence at different
stages:
0x01 = trimmed
0x02 = merged and quality-filtered
0x04 = denoised
0x08 = survived UNCROSS (set later by \code{\link[=add_uncross_to_seq_map]{add_uncross_to_seq_map()}} when
\code{is_tag_jump} is \code{FALSE}; not set here)
}

Bits \code{0x10}--\code{0x80} are reserved for ASV-level filter results
(\code{asv_map$result}: chimera/spike/model), not per-read fate flags.
}
\description{
Analog of \code{\link[=seq_map]{seq_map()}} for the UNOISE path, where merging and quality
filtering happen before denoising.
}
\details{
Bit \code{0x02} (filter) is set when the raw read is present in the merged,
quality-filtered FASTQ. Bit \code{0x04} (denoise & merge) is set when that
merged sequence maps to an ASV in \code{uc}.

Dereplication is repeated here on \code{fq_merged}. That duplicates work already
done inside \code{\link[=vsearch_cluster_unoise2]{vsearch_cluster_unoise2()}}, which is an intentional tradeoff
between computation (dereplication is fast) and storage (the dereplication
map is large).
}
