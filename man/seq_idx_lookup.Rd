% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_map.R
\name{seq_idx_lookup}
\alias{seq_idx_lookup}
\title{Precompute a sequence-to-index lookup for a master ASV list}
\usage{
seq_idx_lookup(queries, seq_all, rc = FALSE)
}
\arguments{
\item{queries}{(\code{character}) query sequences; \code{NA} values are dropped}

\item{seq_all}{(\code{character} vector of sequences,
\code{\link[Biostrings:XStringSet-class]{XStringSet}}, or a readable FASTA path)
unique ASV sequences}

\item{rc}{(\code{logical}) if \code{TRUE}, reverse-complement \code{queries} before
matching. Stored on the result and checked by \code{\link[=match_to_seq_all]{match_to_seq_all()}}.}
}
\value{
object of class \code{seq_idx_lookup} with elements \code{keys}, \code{idx}, and
\code{rc}
}
\description{
Matching against a large \code{seq_all} (millions of ASVs) is dominated by
hashing the table, not by the number of queries. Call this once per chunk
with all unique query sequences, then pass the result to
\code{\link[=match_to_seq_all]{match_to_seq_all()}} for per-sample lookups.
}
\keyword{internal}
