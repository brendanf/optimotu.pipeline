% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uncross.R
\name{add_uncross_to_read_map}
\alias{add_uncross_to_read_map}
\title{Add uncrossing information to a read map}
\usage{
add_uncross_to_read_map(read_map, seqtable_raw, uncross)
}
\arguments{
\item{read_map}{(\code{data.frame}) read map, as returned by \code{\link[=dada2_read_map]{dada2_read_map()}}
or \code{\link[=unoise_read_map]{unoise_read_map()}}}

\item{seqtable_raw}{(\code{data.frame}) sequence table passed to
\code{\link[=remove_tag_jumps]{remove_tag_jumps()}}. Used to rebuild join keys when \code{uncross} does not
contain \code{seq_idx}.}

\item{uncross}{(\code{data.frame}) uncrossing information, as returned by
\code{remove_tag_jumps()}.}
}
\value{
\code{data.frame} with the same columns as \code{read_map}, but with the
\code{flags} column updated to include the \code{is_tag_jump} information from
\code{uncross}.
}
\description{
Sets bit \code{0x08} when the read's current \code{seq_idx} is present in \code{uncross}
for that sample and \code{is_tag_jump} is \code{FALSE}. Join-miss and tag-jumps both
leave \code{0x08} unset. Extra columns on \code{read_map} (e.g. \code{prelulu_idx}) are
preserved.
}
\details{
When \code{uncross} includes \code{seq_idx} (the default after
\code{\link[=remove_tag_jumps]{remove_tag_jumps()}}), the join uses that column. Otherwise keys are rebuilt
from \code{seqtable_raw} by row position.

Call this after \code{\link[=add_lulu_to_read_map]{add_lulu_to_read_map()}} so \code{seq_idx} is the LULU parent
when LULU is enabled.
}
\seealso{
\code{\link[=with_read_map_annotate]{with_read_map_annotate()}}, \code{\link[=add_lulu_to_read_map]{add_lulu_to_read_map()}}
}
