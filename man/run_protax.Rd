% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/protax.R
\name{run_protax}
\alias{run_protax}
\title{Taxonomically identify sequences using the PERL implementation of ProtaxFungi}
\usage{
run_protax(
  seqs,
  outdir,
  modeldir,
  ncpu = local_cpus(),
  seqs_file = NULL,
  seq_idx = NULL,
  script = "scripts/runprotax"
)
}
\arguments{
\item{seqs}{(\code{\link[Biostrings:XStringSet-class]{DNAStringSet}}, \code{character}
vector, \code{\link[fastqindexr:create_index]{fastqindexr_index}}, or \code{data.frame})
sequences to taxonomically identify. If a \code{character} vector, it may be
literal sequences (in which case they should typically be named) or file
paths; files may be either FASTA (possibly gzipped) or \code{.fqi} indexes for
\code{\link[fastqindexr:read_fqi_index]{fastqindexr::read_fqi_index()}}. A single fasta file named \code{all.fa} in the
\code{outdir} is a special case which will be preserved; otherwise this file will
be created as a temporary file and deleted on exit.}

\item{outdir}{(\code{character} string) directory to write output to. If the
directory exists, all existing files in it will be deleted.}

\item{modeldir}{(\code{character} string) directory containing trained model files
for ProtaxFungi}

\item{ncpu}{(\code{integer}) number of threads to use for calls to USEARCH}

\item{seqs_file}{(\code{character} vector) optional per-file paths overriding
those stored in the index, if \code{seqs} is a
\code{\link[fastqindexr:create_index]{fastqindexr_index}} object or \code{.fqi}
path(s). Useful after moving inputs or for \code{targets} dependency tracking.}

\item{seq_idx}{(\code{integer} vector) optional 1-based indices into the logical
sequence stream (\code{NULL} means all sequences in order). Applies after
concatenating multiple FASTA inputs, and supports duplicates and
reordering.}

\item{script}{(\code{character} string) path to the ProtaxFungi script}
}
\value{
a \code{character} vector of output files generated by protax
}
\description{
Taxonomically identify sequences using the PERL implementation of ProtaxFungi
}
