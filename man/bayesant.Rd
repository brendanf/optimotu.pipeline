% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayesant.R
\name{bayesant}
\alias{bayesant}
\title{Identify sequences using BayesANT}
\usage{
bayesant(
  query,
  model,
  ncpu = local_cpus(),
  id_is_int = FALSE,
  n_top_taxa = 20,
  min_prob = 0.01,
  file = NULL,
  seq_idx = NULL,
  ...
)
}
\arguments{
\item{query}{(\code{character} vector, \code{data.frame}, \code{\link[Biostrings:XStringSet-class]{DNAStringSet}},
or \code{character} file name) the sequences to identify. If a file name, it
should point to a FASTA file (possibly gzipped).}

\item{model}{(\code{character} file name or \code{BayesANT} model) the BayesANT model
to use.  If a file name, it must be in .rds or .qs2 format.  Legacy \code{.qs}
files are no longer supported; convert to \code{.qs2} or \code{.rds}.}

\item{ncpu}{(\code{integer}) the number of CPU cores to use}

\item{id_is_int}{(\code{logical}) if \code{TRUE}, parse the sequence IDs as integers}

\item{n_top_taxa}{(\code{integer}) the number of top taxa to return}

\item{min_prob}{(\code{numeric}) the minimum probability to return}

\item{file}{(\code{character} vector) optional per-file paths overriding those
stored in the index, if \code{query} is a
\code{\link[fastqindexr:create_index]{fastqindexr_index}} object or \code{.fqi} /
\code{.qs2} path(s). Useful after moving inputs or for \code{targets} dependency
tracking.}

\item{seq_idx}{(\code{integer} vector) optional 1-based indices into the logical
sequence stream (\code{NULL} means all sequences in order). Applies after
concatenating multiple FASTA inputs, and supports duplicates and
reordering.}

\item{...}{ignored; reserved for \code{targets} dependency tracking (e.g.
\code{hash = seqbatch_hash}) without changing behavior.}
}
\value{
a \code{data.frame} with columns \code{seq_id} (or \code{seq_idx} if \code{id_is_int} is
\code{TRUE}), \code{rank}, \code{parent_taxonomy}, \code{taxon}, and \code{prob}, where \code{seq_id}
(\code{seq_idx}) is the ID of a sequence from \code{query}, \code{rank} is the taxonomic
rank at which the prediction is being made, \code{parent_taxonomy} is the parent
taxonomic unit of the taxon at the previous rank, \code{taxon} is the name of the
predicted taxon, and \code{prob} is the probability of the predicted taxon being
the correct classification of the sequence from \code{query} at the current rank.
If \code{query} is empty, returns an empty table with the same column schema.
}
\description{
Identify sequences using BayesANT
}
