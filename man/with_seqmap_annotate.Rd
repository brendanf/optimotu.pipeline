% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dada2_map.R
\name{with_seqmap_annotate}
\alias{with_seqmap_annotate}
\title{Pipe LULU and/or UNCROSS annotations onto a fate-map expression}
\usage{
with_seqmap_annotate(
  seqmap_expr,
  lulu_map = quote(lulu_asv_map),
  seqtable = NULL,
  uncross = quote(uncross)
)
}
\arguments{
\item{seqmap_expr}{(\code{language}) quoted expression that produces a fate
map as from \code{\link[=seq_map]{seq_map()}} or \code{\link[=unoise_seq_map]{unoise_seq_map()}}.}

\item{lulu_map}{(\code{language}) expression for the LULU map target.
Default \code{lulu_asv_map}.}

\item{seqtable}{(\code{language} or \code{NULL}) community table passed to
\code{\link[=remove_tag_jumps]{remove_tag_jumps()}}. Default \code{seqtable_lulu} when LULU is enabled,
otherwise \code{seqtable_raw}.}

\item{uncross}{(\code{language}) expression for the UNCROSS target. Default
\code{uncross}.}
}
\value{
(\code{language}) \code{seqmap_expr}, possibly piped through LULU and
UNCROSS annotations.
}
\description{
Plan-time helper for \code{targets} commands. Wraps \code{seqmap_expr} in
\code{\link[=add_lulu_to_seq_map]{add_lulu_to_seq_map()}} and/or \code{\link[=add_uncross_to_seq_map]{add_uncross_to_seq_map()}} according to
\code{\link[=do_lulu]{do_lulu()}} and \code{\link[=do_tag_jump]{do_tag_jump()}}.
}
\details{
The result is spliced into a target command with \verb{!!}:

\if{html}{\out{<div class="sourceCode">}}\preformatted{tar_target(dada_map, !!with_seqmap_annotate(quote(seq_map(...))))
}\if{html}{\out{</div>}}

Built with \code{\link[base:substitute]{base::substitute()}} rather than \code{rlang::expr(!!x |> f())}.
In a pipe, \verb{!!} is parsed as two \code{!} operators, so that form would run
the annotation at plan time instead of capturing an expression.
}
\seealso{
\code{\link[=add_lulu_to_seq_map]{add_lulu_to_seq_map()}}, \code{\link[=add_uncross_to_seq_map]{add_uncross_to_seq_map()}}
}
