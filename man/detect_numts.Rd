% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/numt.R
\name{detect_numts}
\alias{detect_numts}
\title{Detect Nuclear Mitochondrial Paralogs (NUMTs)}
\usage{
detect_numts(a2m, id_is_int = FALSE, files = NULL, seq_idx = NULL, ...)
}
\arguments{
\item{a2m}{(\code{character}) name of an A2M file containing a multiple sequence
alignment as generated by \code{hmmalign()}.  In particular, insertions relative
to the reference alignment should be present as masked (lower-case)
characters.}

\item{id_is_int}{(\code{logical}) if \code{TRUE}, return the sequence index as an
integer}

\item{files}{(\code{character} vector) optional per-file paths overriding those
stored in the index, if \code{a2m} is a
\code{\link[fastqindexr:create_index]{fastqindexr_index}} object or \code{.fqi}
path(s). Useful after moving inputs or for \code{targets} dependency tracking.}

\item{seq_idx}{(\code{integer} vector) optional 1-based indices into the logical
sequence stream (\code{NULL} means all sequences in order). Applies after
concatenating multiple FASTA inputs, and supports duplicates and
reordering.}

\item{...}{currently unused; reserved for future extensions.}
}
\value{
\code{data.frame} with columns \code{seq_id} (or \code{seq_idx} if \code{id_is_int} is
\code{TRUE}), \code{numt_indicator}, \code{pos}, and \code{len}, where \code{numt_indicator} is one of
"frameshift" or "stop_codon", \code{pos} is the 1-based position of the indicator,
and \code{len} is the length of the indicator.
}
\description{
Detect Nuclear Mitochondrial Paralogs (NUMTs)
}
