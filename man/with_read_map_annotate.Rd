% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_map.R
\name{with_read_map_annotate}
\alias{with_read_map_annotate}
\title{Pipe LULU and/or UNCROSS annotations onto a read-map expression}
\usage{
with_read_map_annotate(
  read_map_expr,
  lulu_map = quote(lulu_seq_map),
  seqtable = NULL,
  uncross = quote(uncross)
)
}
\arguments{
\item{read_map_expr}{(\code{language}) quoted expression that produces a fate
map as from \code{\link[=dada2_read_map]{dada2_read_map()}} or \code{\link[=unoise_read_map]{unoise_read_map()}}.}

\item{lulu_map}{(\code{language}) expression for the LULU map target.
Default \code{lulu_seq_map}.}

\item{seqtable}{(\code{language} or \code{NULL}) community table passed to
\code{\link[=remove_tag_jumps]{remove_tag_jumps()}}. Default \code{seqtable_lulu} when LULU is enabled,
otherwise \code{seqtable_raw}.}

\item{uncross}{(\code{language}) expression for the UNCROSS target. Default
\code{uncross}.}
}
\value{
(\code{language}) \code{read_map_expr}, possibly piped through LULU and
UNCROSS annotations.
}
\description{
Plan-time helper for \code{targets} commands. Wraps \code{read_map_expr} in
\code{\link[=add_lulu_to_read_map]{add_lulu_to_read_map()}} and/or \code{\link[=add_uncross_to_read_map]{add_uncross_to_read_map()}} according to
\code{\link[=do_lulu]{do_lulu()}} and \code{\link[=do_tag_jump]{do_tag_jump()}}.
}
\details{
The result is spliced into a target command with \verb{!!}:

\if{html}{\out{<div class="sourceCode">}}\preformatted{tar_target(read_map, !!with_read_map_annotate(quote(dada2_read_map(...))))
}\if{html}{\out{</div>}}

Built with \code{\link[base:substitute]{base::substitute()}} rather than \code{rlang::expr(!!x |> f())}.
In a pipe, \verb{!!} is parsed as two \code{!} operators, so that form would run
the annotation at plan time instead of capturing an expression.
}
\seealso{
\code{\link[=add_lulu_to_read_map]{add_lulu_to_read_map()}}, \code{\link[=add_uncross_to_read_map]{add_uncross_to_read_map()}}
}
