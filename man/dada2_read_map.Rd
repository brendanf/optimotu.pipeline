% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dada2_map.R
\name{dada2_read_map}
\alias{dada2_read_map}
\title{Map the fate of individual reads through DADA2 denoising and merge}
\usage{
dada2_read_map(
  sample,
  fq_raw,
  fq_trim,
  fq_filt,
  dadaF,
  derepF,
  dadaR,
  derepR,
  merged,
  denoise_map
)
}
\arguments{
\item{sample}{(\code{character}) sample name(s); length 0 is allowed and
returns an empty fate map}

\item{fq_raw}{(\code{character}) raw FASTQ R1 file path(s)}

\item{fq_trim}{(\code{character}) trimmed FASTQ R1 file path(s)}

\item{fq_filt}{(\code{character}) filtered FASTQ R1 file path(s)}

\item{dadaF}{(\code{\link[dada2:dada-class]{dada2::dada}} or list of such) denoised R1}

\item{derepF}{(\code{\link[dada2:derep-class]{dada2::derep}} or list of such) dereplicated R1}

\item{dadaR}{(\code{\link[dada2:dada-class]{dada2::dada}} or list of such) denoised R2}

\item{derepR}{(\code{\link[dada2:derep-class]{dada2::derep}} or list of such) dereplicated R2}

\item{merged}{(\code{data.frame} as returned by \code{dada2::mergePairs()}, or a named
list of such) result of merging \code{dadaF} and \code{dadaR}}

\item{denoise_map}{(\code{data.frame}) as returned by \code{\link[=make_denoise_map]{make_denoise_map()}} for
the same \code{merged} object(s); must include \code{denoise_idx} and \code{seq_idx},
and \code{sample} when mapping more than one sample}
}
\value{
\code{data.frame} with columns:
\itemize{
\item \code{sample} (character) the sample name
\item \code{raw_idx} (integer) the index of the sequence in the raw file
\item \code{seq_idx} (integer) the index of the sequence in \code{seq_all}
\item \code{flags} (raw) bitset indicating the presence of the sequence at different
stages:
0x01 = trimmed
0x02 = filtered
0x04 = denoised & merged
0x08 = survived UNCROSS (set later by \code{\link[=add_uncross_to_read_map]{add_uncross_to_read_map()}} when
\code{is_tag_jump} is \code{FALSE}; not set here)
}

Bits \code{0x10}--\code{0x80} are reserved for ASV-level filter results
(\code{asv_map$result}: chimera/spike/model), not per-read fate flags.
}
\description{
Accepts a single sample or a chunk of samples. Sequence-to-index matching
against \code{seq_all} must already have been done by \code{\link[=make_denoise_map]{make_denoise_map()}};
pass that result as \code{denoise_map}. A length-0 \code{sample} (empty seq batch)
returns a 0-row fate map with the usual columns.
}
