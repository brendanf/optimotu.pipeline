% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lulu_long.R
\name{add_lulu_to_read_map}
\alias{add_lulu_to_read_map}
\title{Remap a per-read fate map through LULU parent assignment}
\usage{
add_lulu_to_read_map(read_map, lulu_map)
}
\arguments{
\item{read_map}{(\code{data.frame}) read map, as returned by \code{\link[=dada2_read_map]{dada2_read_map()}}
or \code{\link[=unoise_read_map]{unoise_read_map()}}}

\item{lulu_map}{(\code{data.frame}) output of \code{\link[=lulu_map]{lulu_map()}}, with columns
\code{seq_idx} and \code{lulu_idx}}
}
\value{
\code{data.frame} with the same columns as \code{read_map}, plus \code{prelulu_idx}
(integer). \code{seq_idx} is the LULU parent where a mapping exists.
}
\description{
Rewrites \code{seq_idx} from the denoise-time OTU id to the LULU parent id so it
matches post-LULU community tables (\code{seqtable_lulu}, \code{seqtable_uncross}).
The original denoise-time id is kept as \code{prelulu_idx}. A LULU daughter is
\code{!is.na(prelulu_idx) && prelulu_idx != seq_idx}. No flag bit is set; bits
\code{0x10}--\code{0x80} are reserved for ASV-level filter results.
}
\details{
Never-denoised rows (\code{seq_idx} \code{NA}) are left unchanged. Denoise ids missing
from \code{lulu_map} keep their original \code{seq_idx}.
}
\seealso{
\code{\link[=with_read_map_annotate]{with_read_map_annotate()}}, \code{\link[=add_uncross_to_read_map]{add_uncross_to_read_map()}}
}
