% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lulu_long.R
\name{add_lulu_to_seq_map}
\alias{add_lulu_to_seq_map}
\title{Remap a per-read fate map through LULU parent assignment}
\usage{
add_lulu_to_seq_map(seqmap, lulu_map)
}
\arguments{
\item{seqmap}{(\code{data.frame}) sequence map, as returned by \code{\link[=seq_map]{seq_map()}} or
\code{\link[=unoise_seq_map]{unoise_seq_map()}}}

\item{lulu_map}{(\code{data.frame}) output of \code{\link[=lulu_map]{lulu_map()}}, with columns
\code{seq_idx} and \code{lulu_idx}}
}
\value{
\code{data.frame} with the same columns as \code{seqmap}, plus \code{denoise_idx}
(integer). \code{seq_idx} is the LULU parent where a mapping exists.
}
\description{
Rewrites \code{seq_idx} from the denoise-time ASV id to the LULU parent id so it
matches post-LULU community tables (\code{seqtable_lulu}, \code{seqtable_uncross}).
The original denoise-time id is kept as \code{denoise_idx}. A LULU daughter is
\code{!is.na(denoise_idx) && denoise_idx != seq_idx}. No flag bit is set; bits
\code{0x10}--\code{0x80} are reserved for ASV-level filter results.
}
\details{
Never-denoised rows (\code{seq_idx} \code{NA}) are left unchanged. Denoise ids missing
from \code{lulu_map} keep their original \code{seq_idx}.
}
